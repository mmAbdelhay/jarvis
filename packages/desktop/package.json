{
  "name": "@jarvis/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/src/main.js",
  "scripts": {
    "build": "tsc -b ../../tsconfig.json && node scripts/copy-vendor.mjs",
    "start": "pnpm run build && electron ."
  },
  "dependencies": {
    "@jarvis/core": "workspace:*",
    "@jarvis/platform": "workspace:*",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "electron": "^44.0.0"
  }
}
