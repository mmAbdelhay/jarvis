{
  "name": "@jarvis/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -b",
    "start": "pnpm run build && electron ."
  },
  "dependencies": {
    "@jarvis/core": "workspace:*",
    "@jarvis/platform": "workspace:*"
  },
  "devDependencies": {
    "electron": "^44.0.0"
  }
}
